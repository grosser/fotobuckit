<h1>Edit your account</h1>

<% form_for current_user, :html => {:class => "full_form"}  do |f| %>
  <%= render 'form', :f => f %>
  <%= render 'bucket_fields', :f => f %>
  <%= submit_tag 'Save' %>
<% end %>
